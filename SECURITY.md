# Security

AI Agent Debug Kit runs locally in the browser and does not send logs to a server.

Still, logs may contain sensitive data. Before sharing screenshots, reports, or exported Markdown:

- remove API keys
- remove access tokens
- remove passwords
- remove private repository names if needed
- remove customer or user personal data

If you find a security issue in this project, open a GitHub issue with a minimal reproduction and do not include real secrets.
