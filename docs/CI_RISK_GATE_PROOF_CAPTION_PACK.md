# CI Risk Gate Proof Caption Pack

Use these captions to promote the public CI risk gate portfolio proof without claiming private client work.

Source proof:

- `docs/PORTFOLIO_CASE_CI_RISK_GATE.md`
- `docs/CI_RISK_GATE_ONE_PAGER.md`
- `docs/examples/ci-risk-gate-sample-pair.md`
- Public demo: https://butianhua4.github.io/ai-agent-debug-kit/

## Fiverr Portfolio Caption

```text
Public demo case: AI Agent CI Risk Gate

This proof sheet shows how a lightweight CI gate can flag risky AI agent logs before merge. The demo uses synthetic pass/fail logs to show repeated tool failures, retry loops, permission errors, secret-like strings, and high-risk scores.

No private client data is used. The delivery style is copy-paste friendly: CI workflow step, threshold notes, sample pass/fail behavior, and warning-mode rollout guidance.
```

## Fiverr Profile Short Blurb

```text
I build lightweight AI automation debugging tools, CI risk gates, and redacted log reports. My work focuses on safe, copy-paste delivery: no secrets, no direct repo login for first scope, and clear handoff notes for developers.
```

## Buyer DM Snippet

```text
I have a public CI risk gate demo that shows the exact delivery style: redacted/synthetic logs, pass/fail behavior, threshold notes, and warning-mode rollout. If you share your CI provider plus a redacted sample, I can route the scope into Starter, Standard, or Premium.
```

## LinkedIn / GitHub Profile Post

```text
I added a public demo case for an AI Agent CI Risk Gate.

The idea is simple: if an agent run produces risky logs, CI should catch the signal before merge. The demo uses synthetic pass/fail logs to show checks for repeated tool failures, retry loops, permission errors, secret-like strings, and high-risk scores.

It is intentionally lightweight:
- copy-paste CI workflow step
- warning-mode first
- threshold tuning notes
- Markdown/JSON report examples
- no secrets or direct repo login required for first scope

Demo:
https://butianhua4.github.io/ai-agent-debug-kit/
```

## Short Social Post

```text
New public demo: AI Agent CI Risk Gate.

It turns messy agent logs into a pre-merge warning signal:
- repeated tool failures
- retry loops
- permission errors
- secret-like strings
- high-risk scores

Built for safe handoff: redacted logs, copy-paste CI step, warning mode first.
https://butianhua4.github.io/ai-agent-debug-kit/
```

## Chinese Platform Post

```text
我做了一个公开演示案例：AI Agent CI 风险门禁。

它解决的问题很具体：AI Agent / 自动化流程跑在 CI 里时，日志里可能出现重复工具调用失败、重试循环、权限错误、疑似密钥字符串、高风险分数，但普通 CI 报错只会告诉你“失败了”。

这个 demo 展示的是轻量交付方式：
- 用脱敏/合成日志做 pass/fail 示例
- 给出可复制的 CI workflow step
- 先 warning-only，不直接硬拦截
- 提供阈值建议和 Markdown/JSON 报告样例
- 不需要直接登录私有仓库，不需要密钥

公开演示：
https://butianhua4.github.io/ai-agent-debug-kit/
```

## Chinese Short Caption

```text
AI Agent 跑 CI，不应该只看到“失败了”。

我做了一个轻量 CI 风险门禁 demo：用脱敏/合成日志检查重复工具失败、重试循环、权限错误、疑似密钥和高风险分数。

适合需要“先 warning，再逐步 fail build”的团队。
```

## Hashtags

```text
AI Agent, CI, GitHub Actions, Node.js, Automation, LLMOps, AI Workflow, Debugging
```

## Safe CTA

```text
If your AI agent workflow is failing in CI, send the CI provider, log format, and 10-20 redacted log lines. I can scope a lightweight risk gate without direct repo login or secrets.
```
