# Chinese 299 Xiaohongshu and Xianyu Posts

Use this pack to turn the RMB 299 AI automation diagnosis offer into short posts that can be published on Xiaohongshu, Xianyu, WeChat groups, private communities, Zhihu, Juejin, or direct chat.

Do not promise guaranteed repair, direct account login, payment/KYC/wallet handling, scraping bypass, or platform evasion.

## Xiaohongshu Post 1 - Problem Awareness

Title:

AI 自动化跑不通，先别急着重做，很多只是字段错了

Body:

最近很多人用 AI 搭 n8n、Make、Zapier、Dify、Coze 或 Webhook 自动化，最容易卡在这些地方：

- Webhook 明明收到了数据，后面节点却没反应；
- JSON 字段路径变了，表格/CRM 拿不到值；
- AI Agent 输出格式漂移，下一步解析失败；
- 重试以后重复发送，客户收到多条消息；
- API 报错，但不知道是权限、字段还是格式问题。

这种情况不一定要马上找人重做整套系统。更稳的做法是先做一次故障诊断，把失败链路、证据和最小测试步骤写清楚。

我这边可以做 299 元 AI 自动化故障诊断：不登录你的后台，不收密码/API Key/Cookie，只看脱敏截图、报错文字、样例输入输出，给你一份中文诊断报告。

适合：自己用 AI 搭了流程但跑不通、看不懂报错、想先低成本判断问题范围的人。

不适合：要求我直接登录账号代操作、处理付款/KYC/钱包/税务、或者 299 就保证修好整套系统。

想判断你的情况适不适合，可以先发：平台名称 + 报错截图 + 你希望输出什么结果。

First comment:

诊断报告会写清：失败点在哪里、最可能原因、证据、最小测试步骤、是否适合升级 999 小范围修复方案。

## Xiaohongshu Post 2 - Non-Technical Buyer

Title:

不懂代码也能排查 AI 工作流报错吗？可以，但材料要给对

Body:

很多人不是程序员，但已经开始用 AI 自动化做内容、私域、表格、CRM、客服、线索同步。

真正难的不是“会不会写代码”，而是报错以后不知道怎么描述问题：

- 哪一步失败？
- 输入是什么？
- 期望输出是什么？
- 实际输出哪里不对？
- 是字段错、权限错、格式错，还是重复触发？

所以我做了一个 299 元的 AI 自动化故障诊断入口。你不用给账号密码，也不用发密钥，只需要发脱敏材料：

1. 失败节点截图；
2. 报错文字；
3. 一条脱敏输入样例；
4. 你希望最终输出的样子；
5. 使用的平台：n8n / Make / Zapier / Dify / Coze / 自写脚本 / 其他。

我会整理成一份中文诊断报告，帮你判断问题卡在哪里、下一步应该怎么测、是否值得继续修。

如果只是想让我“直接登录进去帮你点”，这个服务不接。这样对你的账号和数据更安全。

First comment:

如果你不知道怎么脱敏，可以先把姓名、手机号、邮箱、订单号、Token、Cookie、付款信息全部打码。

## Xiaohongshu Post 3 - Price Ladder

Title:

AI 自动化修不好时，299 / 999 / 1999 分别适合什么情况？

Body:

为了避免一上来就把小问题做成大项目，我把 AI 自动化故障处理拆成三个层级：

299 元：故障诊断报告

适合你已经有报错、截图、样例输入输出，但不知道问题卡在哪里。交付一份中文诊断报告，告诉你失败点、可能原因、证据和最小测试步骤。

999 元：小范围修复方案

适合诊断后确认范围比较小，比如一个 JSON 字段映射、一个 Webhook 触发、一个输出格式、一个节点配置。交付修复方案、测试步骤和回滚建议。

1999+ 元：复杂救援拆解

适合多流程、生产风险、重复发送、成本异常、长时间不稳定、多个平台串联的问题。需要重新拆范围，不按小单处理。

我不接：账号密码、API Key、Cookie、KYC、付款、钱包、税务、绕过平台风控。

如果你只是“不知道这个报错该不该继续修”，先从 299 诊断开始就够了。

First comment:

发材料前先脱敏：隐藏账号、手机号、客户数据、Token、Cookie、付款信息。

## Xianyu Listing Titles

Choose one:

1. AI 自动化报错诊断 n8n/Make/Zapier/Webhook/JSON 排查
2. AI Agent 工作流失败排查 299 元中文诊断报告
3. Webhook API JSON 字段映射报错排查 自动化故障诊断
4. Dify Coze n8n Make Zapier 自动化流程跑不通排查
5. AI 工作流重复发送 字段错乱 API 报错 诊断报告

## Xianyu Listing Body

你的 AI 自动化、n8n、Make、Zapier、Dify、Coze、Webhook、API 或 JSON 字段映射跑不通，我可以先帮你做一份 299 元中文故障诊断报告。

注意：这个服务不是直接登录你的后台代操作，也不是 299 保证修好整套系统。它的价值是先把问题范围、失败证据和下一步测试路径找出来，避免你盲目重做或乱改。

适合这些情况：

- Webhook 收到数据，但后面节点没有正确执行；
- JSON 字段路径变化，表格/CRM/飞书/Notion 拿不到值；
- AI Agent 输出格式不稳定，后续解析失败；
- Make / Zapier / n8n 重试后重复发送；
- API 调用失败，不确定是权限、字段还是格式；
- 自动化成本异常，怀疑重复触发或循环调用；
- 自己用 AI 写了脚本或流程，但不知道报错怎么排查。

你需要提供：

1. 失败节点截图，隐藏账号和敏感信息；
2. 报错文字，能复制就复制；
3. 一条脱敏输入样例；
4. 你希望最终输出的结果；
5. 使用的平台名称。

我会交付：

- 当前失败点判断；
- 最可能的 1-3 个原因；
- 截图/报错能说明什么；
- 最小测试步骤；
- 哪些地方不要先乱改；
- 适合停在 299、升级 999 小范围修复方案，还是按 1999+ 复杂救援重新拆范围。

不需要也不要发：

- 账号密码；
- API Key / Token / Cookie；
- 客户名单；
- 付款、钱包、KYC、税务信息；
- 生产后台权限。

价格说明：

- 299 元：故障诊断报告；
- 999 元：诊断后的修复方案和测试步骤；
- 1999+ 元：多流程、生产风险、重复发送、成本异常等复杂救援。

下单前可以先发一张脱敏报错截图，我会先判断是否适合做 299 诊断。

## Xianyu First Reply

可以，先不要发账号密码、API Key、Token 或 Cookie。

你按这个格式发脱敏材料就行：

1. 你用的平台：n8n / Make / Zapier / Dify / Coze / 自写脚本 / 其他；
2. 哪一步失败；
3. 报错截图或报错文字；
4. 一条脱敏输入样例；
5. 你希望输出什么结果。

我先判断适合 299 诊断、999 修复方案，还是需要重新拆复杂范围。

## WeChat Group Short Post

有用 AI / n8n / Make / Zapier / Dify / Coze 搭自动化但跑不通的，可以先做 299 元故障诊断。

不登录后台，不收密码/API Key/Cookie，只看脱敏截图、报错文字、样例输入输出，交付一份中文诊断报告：失败点、可能原因、证据、最小测试步骤、下一步是否值得修。

适合字段映射错、Webhook 不通、AI 输出格式漂移、重复发送、API 报错、自动化成本异常这类问题。

需要的话先发脱敏截图，我判断是否适合诊断。

## Posting Order

1. Publish Xianyu listing with `assets/domestic-299-diagnosis-cover.png`.
2. Add `assets/domestic-299-diagnosis-proof.png` as the second image.
3. Add `assets/domestic-299-price-ladder.png` as the third image.
4. Use Xiaohongshu Post 1 first because it explains the pain clearly.
5. Use Xiaohongshu Post 3 later as a package-selection post.
6. Use the WeChat group short post only in groups where service posts are allowed.

## Safety Reminder

Before posting, remove any claim that implies:

- guaranteed repair;
- direct login operation;
- platform bypass;
- handling payment, wallet, KYC, tax, or private credentials;
- use of private customer data.
