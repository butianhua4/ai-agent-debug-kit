{
  "name": "ai-agent-debug-kit",
  "version": "0.5.0",
  "description": "Zero-dependency browser and CLI tool for turning AI agent logs into readable debug reports.",
  "private": true,
  "scripts": {
    "check": "node --check core.js && node --check script.js && node --check cli.js && node --check extension/popup.js && node tests/smoke.test.js && node scripts/validate-extension.js",
    "build:release": "node scripts/build-release.js",
    "preflight": "npm run check && npm run build:release && node scripts/validate-release.js",
    "report": "node cli.js sample-agent-log.jsonl"
  },
  "bin": {
    "agent-debug-kit": "./cli.js"
  },
  "keywords": [
    "ai-agent",
    "llm",
    "debugging",
    "developer-tools",
    "logs",
    "jsonl"
  ],
  "license": "MIT"
}
